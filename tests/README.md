# Tests

This directory will contain automated tests for public behavior, contracts, safety invariants and regression fixtures.

Milestone M0 adds no restoration tests because no restoration behavior exists yet. Repository-structure validation is performed by `tools/validate_repository.py`.
