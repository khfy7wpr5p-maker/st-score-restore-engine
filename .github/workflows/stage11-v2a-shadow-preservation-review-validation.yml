name: Stage 11 V2a shadow preservation review validation

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate-stage11-v2a-shadow-preservation-review:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: src
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install exact binary runtime
        run: |
          python -m pip install \
            --disable-pip-version-check \
            --only-binary=:all: \
            --no-deps \
            -r requirements.lock

      - name: Validate preservation evidence, cross-run identity, and current truth
        run: python tools/validate_stage11_v2a_shadow_preservation_review.py

      - name: Run preservation review tests
        run: python -m unittest tests.test_stage11_v2a_shadow_preservation_review -v

      - name: Compile preservation review sources
        run: |
          python -m py_compile \
            src/st_score_restore/stage11_v2a_shadow_preservation_review.py \
            src/st_score_restore/stage11_v2a_shadow_preservation_review_current_truth.py \
            tools/run_stage11_v2a_shadow_preservation_review.py \
            tools/validate_stage11_v2a_shadow_preservation_review.py

      - name: Verify committed JSON artifacts
        run: |
          python -m json.tool evidence/stage11/v2a/v2a-shadow-preservation-review-evidence.v1.json > /dev/null
          python -m json.tool docs/live/ST_SCORE_RESTORE_STAGE11_V2A_SHADOW_PRESERVATION_REVIEW_CURRENT_TRUTH.json > /dev/null

      - name: Verify production and normal application surfaces stay untouched
        shell: bash
        run: |
          BASE="${{ github.event.pull_request.base.sha || github.event.before }}"
          CHANGED="$(git diff --name-only "$BASE" HEAD)"
          if echo "$CHANGED" | grep -E '^(src/st_score_restore/http_api.py|api/openapi.v1.json|tools/run_api.py|src/st_score_restore/job_service.py)$'; then
            echo "Stage 11 preservation review must not modify public/normal application surfaces"
            exit 1
          fi
